<UserControl x:Class="MoneyManager.Views.BankOperations.BankOperationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewmodels="clr-namespace:MoneyManager.ViewModels.BankOperations"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Список банковских операций -->
        <ListView ItemsSource="{Binding BankOperations}" SelectedItem="{Binding SelectedBankOperation}" Grid.Column="0" Grid.Row="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding OperationId}" Width="50"/>
                    <GridViewColumn Header="Amount" DisplayMemberBinding="{Binding OperationAmount}" Width="100"/>
                    <GridViewColumn Header="Date" Width="100">
                        <GridViewColumn.DisplayMemberBinding>
                            <Binding Path="OperationDate" StringFormat="{}{0:dd.MM.yyyy}" />
                        </GridViewColumn.DisplayMemberBinding>
                    </GridViewColumn>
                    <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}" Width="200"/>
                    <GridViewColumn Header="Account" DisplayMemberBinding="{Binding Account.AccountName}" Width="150"/>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding OperationType}" Width="100"/>
                    <GridViewColumn Header="Subcategory" DisplayMemberBinding="{Binding Subcategory.SubcategoryName}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Кнопки управления -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="1" Margin="10">
            <Button Content="Add" Command="{Binding ShowAddBankOperationViewCommand}" Margin="5"/>
            <Button Content="Edit" Command="{Binding EditBankOperationCommand}" Margin="5" IsEnabled="{Binding IsBankOperationSelected}"/>
            <Button Content="Delete" Command="{Binding DeleteBankOperationCommand}" Margin="5" IsEnabled="{Binding IsBankOperationSelected}"/>
        </StackPanel>

        <!-- ContentControl для отображения Add/Edit BankOperationView -->
        <ContentControl Grid.Column="1" Grid.RowSpan="2" Content="{Binding CurrentViewModel}" />
    </Grid>
</UserControl>
