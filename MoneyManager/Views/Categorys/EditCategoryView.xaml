<UserControl x:Class="MoneyManager.Views.Categorys.EditCategoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:MoneyManager.Views"
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Название категории:" Grid.Row="0" VerticalAlignment="Center" />
        <TextBox Text="{Binding EditedCategory.CategoryName}" Grid.Row="0" Margin="112,14,0,10" />

        <Button Content="Изменить категорию" Command="{Binding EditCategoryCommand}" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,111,0" />
    </Grid>
</UserControl>
