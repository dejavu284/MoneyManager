<UserControl x:Class="MoneyManager.Views.AddAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="300">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Account Balance:" Grid.Row="0" VerticalAlignment="Center" />
        <TextBox Text="{Binding NewAccount.AccountBalance}" Grid.Row="0" Margin="10,0,0,10" />

        <TextBlock Text="Currency ID:" Grid.Row="1" VerticalAlignment="Center" />
        <TextBox Text="{Binding NewAccount.CurrencyId}" Grid.Row="1" Margin="10,0,0,10" />

        <Button Content="Add Account" Command="{Binding AddAccountCommand}" Grid.Row="2" HorizontalAlignment="Right" Margin="10,10,0,0" />
    </Grid>
</UserControl>
